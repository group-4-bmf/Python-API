name: push-toaws.yml
on: [push]
jobs:
  docker-compose:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: docker-compose up
      - run: python3 install pytest
      - run: cd testing/
      - run: pytest